version: '3'

services:
    nx-app-base:
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        environment:
            - DEV_PLATFORM=DOCKER
    nx-angular-base:
        build:
            context: .
            dockerfile: Angular.Dockerfile
        environment:
            - DEV_PLATFORM=DOCKER
